import { test, expect } from './../BaseTestFile';

test('Filter on startpage', async ({ StorefrontPage, AdminApiContext, DefaultSalesChannel }) => {
    await StorefrontPage.goto('/');

    // Filter for manufacturer shopwareAG
    // ...
});

test('Filter on startpage 2 with revenge', async ({ StorefrontPage, AdminApiContext, DefaultSalesChannel }) => {
    await StorefrontPage.goto('/');

    // Filter for manufacturer shopwareAG
    // ...
});
